ChatClaud FIX v10 — поверх ТВОИХ рабочих файлов

Изменения ТОЛЬКО:
1) Кнопка нового чата = спираль icon-192.png (не +)
2) Чёрная полоска Thinking/Searching сильнее
3) Nova: поиск с 4 символов + ключи найди/поищи/мем/tiktok
4) Запрет выдуманных ссылок
5) SW cache v10 (сброс старого кэша)

КАК ЗАЛИТЬ:
1. Залей ВСЕ файлы из ZIP в GitHub/Render (index.html, server.js, sw.js, icon-192.png)
2. Render → Clear build cache → Deploy
3. Телефон: удали PWA с Домой ИЛИ Safari инкогнито
4. Проверка: слева внизу СПИРАЛЬ, не плюс

Если плюс остался — это старый кэш, не новый код.
